<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <include domain="database" path="." />
        <exclude domain="sharedpref" path="sabbora_identity.xml" />
    </cloud-backup>
    <!--
      Device-to-device transfer moves the install to a new phone rather than cloning it,
      so carrying the device id across is correct here.
    -->
    <device-transfer>
        <include domain="database" path="." />
        <include domain="sharedpref" path="sabbora_identity.xml" />
    </device-transfer>
</data-extraction-rules>
